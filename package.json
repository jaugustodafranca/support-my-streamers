{
  "name": "support-my-streamers",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension that rotates between Twitch streamers you follow, lurking a few at a time.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jaugustodafranca/support-my-streamer.git"
  },
  "scripts": {
    "secrets:inject": "node scripts/inject-secrets.mjs",
    "pretest": "node scripts/inject-secrets.mjs",
    "prebuild": "node scripts/inject-secrets.mjs",
    "build": "bash scripts/package.sh",
    "icons:active": "node scripts/generate-active-icons.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "sharp": "^0.35.0",
    "vitest": "^2.1.8"
  }
}
